# bench

`node bench/bench.mjs` — Matra alone, against the built bundle.

Comparing against Tiptap needs it installed, which this repo deliberately does
not do. See BENCHMARKS.md for the method and the caveats; the view numbers come
from happy-dom and are not browser numbers.
