/**
 * matra — the batteries-included entry point.
 *
 * Everything in @matra/core is re-exported here so a first install is one
 * dependency. Reach for @matra/core directly when you want the engine alone.
 */
export * from '@matra/core'
